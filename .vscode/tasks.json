{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with debugging symbols",
            "type": "shell",
            "command": "gcc ${file} -g -Og -ggdb -fno-eliminate-unused-debug-symbols -o ${fileBasenameNoExtension}"
        }
    ]
}